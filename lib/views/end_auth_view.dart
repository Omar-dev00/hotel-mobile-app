import 'package:flutter/material.dart';
import 'package:hotel/widgets/end_auth_view_body.dart';

class EndAuthView extends StatelessWidget {
  const EndAuthView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: EndAuthViewBody(),
    );
  }
}
